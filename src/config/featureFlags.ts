/**
 * Feature flags for incremental backend migration.
 * 
 * HARDCODED FOR TESTING - env variables not loading properly
 */

export const featureFlags = {
  useRealAuth: true,
  useRealSchools: false,
  useRealBranches: false,
  useRealAnalytics: false,
  useRealBilling: false,
  useRealSettings: false,
  useRealOnboarding: false,
};
